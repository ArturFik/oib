const rootDirection = 'auth'
export default {
    ROOT: `${rootDirection}`,
    SIGN_IN: `${rootDirection}-sign-in`,
    SIGN_UP: `${rootDirection}-sign-up`,
    RECOVERY: `${rootDirection}-recovery`,
    LOGOUT: `${rootDirection}-logout`,
}